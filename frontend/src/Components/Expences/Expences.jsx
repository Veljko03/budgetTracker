import styled from "styled-components";
import { InnerLayout } from "../../styles/Layouts";

const Expences = () => {
  return (
    <ExpencesStyled>
      <InnerLayout>Expances</InnerLayout>
    </ExpencesStyled>
  );
};

const ExpencesStyled = styled.div``;

export default Expences;
